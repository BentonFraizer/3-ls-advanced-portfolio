.thumbs {
  position: relative;
  display: flex;
}

.thumbs__item {
  border-top: 3px solid transparent;
  border-bottom: 3px solid transparent;
  &:first-child {
    border: 3px solid #DC9322;
  }
}
