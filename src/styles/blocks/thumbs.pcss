.thumbs {
  position: relative;
  display: flex;
}

.thumbs__item {
  border-top: 3px solid transparent;
  border-bottom: 3px solid transparent;
  
  &:first-child {
    border: 3px solid #ff9a00;
  }
}

.thumbs__pic {
  height: 97px;
  width: 100%;
}